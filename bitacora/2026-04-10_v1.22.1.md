# Bitácora CALLE — 2026-04-10 · v1.22.1 (deploy fix)

## Contexto
La app desplegada se quedó servida en v1.14. Vercel seguía construyendo desde un commit
incompleto (69d8c10) que no tenía la mayoría de los archivos nuevos del ciclo v1.22.

---

## Lo que se hizo

### 1. Actualización visual de versión
- `src/MapboxTracking.tsx:1384` — badge del HUD actualizado de `v1.14` → `v1.22.0`
- Confirmación visual en pantalla para saber exactamente qué build está corriendo.

### 2. Invalidación de caché PWA
- `vite.config.ts` — agregado `cacheId: 'calle-cache-v1-22'` en la sección `workbox`.
- Efecto: renombra el precache store del Service Worker. Cualquier SW antiguo lo detecta
  como store desconocido y descarga el bundle completo desde cero.
- Los caches de runtime (mapbox, supabase) no se tocan — solo el precache de la app.

### 3. Diagnóstico del build roto en Vercel
Dos errores reportados por Vercel (commit 69d8c10):
```
src/App.tsx(161,10): error TS2741 — prop 'userXp' faltante en MapboxTracking
src/hooks/useMissions.ts(5,8): error TS2307 — Cannot find module '../types/missions'
```
Causa raíz identificada: **no eran errores de lógica**. Eran 16 archivos y 4 directorios
enteros que nunca habían sido commiteados al repo de GitHub:
- `src/types/` (missions.ts)
- `src/services/` (handshakeService.ts, missionService.ts)
- `src/utils/` (xpLogic.ts, streakLogic.ts)
- `src/providers/` (PostHogProvider.tsx)
- `src/AuthScreen.tsx`, `src/TribeSelection.tsx`
- `src/components/MissionsScreen.tsx`, `MissionOfDay.tsx`, `JointMissionInviteModal.tsx`
- `src/hooks/useGlobalHandshake.ts`, `useHandshake.ts`, `useHandshakeListener.ts`,
  `useMissionFeedback.ts`, `useUserStats.ts`

### 4. Verificación Realtime
Confirmado que la suscripción Realtime para encuentros está correctamente implementada
en los hooks dedicados (no en EncounterModal directamente, que es código-manual):
- `useHandshakeListener.ts` → canal `hs:rx:{userId}`, tabla `handshake_requests`,
  filtro `receiver_id=eq.${userId}`
- `useGlobalHandshake.ts` → canal `global:hs:rx:{userId}`, mismo filtro

### 5. Commit y push del release completo
```
commit 8cda5b0  →  master
feat: release total v1.22 con realtime y dependencias
30 archivos · +4.067 líneas · 16 archivos nuevos
```

---

## Estado al cierre
- Vercel desplegando desde commit `8cda5b0` con todos los archivos presentes.
- `tsc --noEmit` local: 0 errores.
- Badge en HUD muestra `v1.22.0`.
- PWA forzará descarga limpia a todos los usuarios con SW antiguo.
- Inicio de fase de pruebas en dispositivo real.
